{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "vite",
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "installCommand": "npm install --no-workspaces --no-package-lock --legacy-peer-deps",
  "rewrites": [{ "source": "/(.*)", "destination": "/" }]
}
